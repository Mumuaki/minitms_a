# Конфигурация проекта MiniTMS (единый источник правды)

**При изменении документации и скриптов всегда используйте эти значения.** Не дублируйте другие пути или репозитории.

| Параметр | Значение |
|----------|----------|
| **Размещение приложения на VPS** | `/opt/minitms` |
| **Удалённый репозиторий Git** | `https://github.com/Mumuaki/minitms_a.git` |
| **Ветка по умолчанию** | `main` |
| **Сервер (IP)** | `89.167.70.67` |
| **Пользователь SSH** | `root` |
| **Git на Windows (локально)** | `D:\Git` (исполняемый файл: `D:\Git\cmd\git.exe`) |

## Где это используется

- Все скрипты, подключающиеся по SSH к серверу (путь к проекту на сервере).
- Вся документация: пути к каталогу проекта на VPS, команды `cd`, пути к `.env`, systemd unit'ы, примеры.
- Синхронизация с GitHub: `origin` = minitms_a, на сервере каталог — `/opt/minitms`. Скрипт `scripts/sync-remote-server.ps1` при неудачном `git pull` (конфликты или «untracked would be overwritten») выполняет `git reset --hard origin/main`, чтобы дерево на сервере совпало с репозиторием.
- PowerShell-скрипты запускают Git через `D:\Git\cmd\git.exe` (не полагаются на PATH).

## Если нужно изменить

1. Обновите этот файл.
2. Выполните поиск по проекту по старому значению и замените во всех найденных файлах (документация, `.ps1`, `.sh`, конфиги).

## Архитектурные решения (Модуль 9)

Зафиксированы следующие решения для разрешения противоречий в документации:
1. **Единицы веса:** Хранение и расчеты ведутся в килограммах (кг). Тонны могут использоваться только для отображения в UI.
2. **База данных:** Минимальная версия PostgreSQL зафиксирована как 15+.
3. **Шина сообщений:** Используется Celery + Redis. Интеграция RabbitMQ отменена.
4. **GPS Провайдер:** Основным провайдером является GPS Guard (a1.gpsguard.eu). Wialon/Navixy оставлены как альтернативные.
5. **Цветовые пороги:** Подтверждено, что 4-цветная система рентабельности (€/км) и 3-цветная система отклонений план/факт — это разные, независимые системы.
6. **Telegram:** Интеграция отложена (deferred), функционал не реализуется в базовой версии.
